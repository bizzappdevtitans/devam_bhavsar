<odoo>
    <!-- Inherted form view from product.product and added required fields #T00447 -->
    <record id="product_normal_form_view" model="ir.ui.view">
        <field name="name">product.product.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view" />
        <field name="arch" type="xml">
            <xpath expr="/form/sheet/notebook/page[@name='sales']" position="after">
                <page string="Last 5 purchase orders" name="last_5_purchase_orders">
                    <field name="last_5_po">
                        <tree>
                            <field name="name" />
                            <field name="date_approve" />
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
